{
  "type": "record",
  "namespace": "org.kurron.avro.example",
  "name": "User",
  "doc": "structure representing a user in the system",
  "fields": [
    {
      "name": "name",
      "type": "string",
      "default": "defaulted v110 name",
      "doc": "the user's real world name"
    },
    {
      "name": "username",
      "type": "string",
      "default": "defaulted v110 username",
      "doc": "the user's system login"
    }
  ]
}